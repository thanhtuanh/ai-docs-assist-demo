export const environment = {
  production: true,
  apiUrl: '/api', // ✅ Nginx proxy verwendet
  features: {
    industryDetection: true,
    aiAnalysis: true,
    batchProcessing: true,
    realtimeAnalysis: true
  }
};